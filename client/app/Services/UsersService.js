import store from "../store.js";

const _userApi = axios.create({
  baseURL: "",
  timeout: 3000
})
class UsersService {
  addUserAsync(user) {
    let res = await this.addUserAsync(user) {
    }
  }
  constructor() {}
  
}

const service = new UsersService();
export default service;
